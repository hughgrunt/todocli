compiling
javac -cp lib/*.jar Main.java td-pkg/*.java

running
java -cp ".:tdpkg:lib/*" Main

compile as one package
jar cfm myapp.jar manifest.mf -C . Main.class tdpkg/com/example/*.class

run package
java -jar todo.jar
